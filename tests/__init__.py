# Import all models here on in it so they register to db.Model on init
from tektonik.models import page
from tektonik.models import path
from tektonik.models import path_page
from tektonik.models import property